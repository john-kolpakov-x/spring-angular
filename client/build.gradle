plugins {
  id "com.moowork.node" version "1.2.0"
}

task cleanLibs(type: Delete) {
  delete 'node_modules'
  delete 'dist'
}

clean.dependsOn cleanLibs

task dist {
  group "build"
  dependsOn yarn
  dependsOn npm_run_build
}

npm_run_build.shouldRunAfter yarn
